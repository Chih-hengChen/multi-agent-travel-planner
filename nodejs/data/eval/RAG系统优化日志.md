# RAG 系统优化日志

## 实验环境

| 项目 | 值 |
|------|-----|
| 向量存储 | MemoryVectorStore（文件持久化） |
| 嵌入模型 | 智谱 embedding-3（2048 维） |
| 召回策略 | 向量余弦相似度 → threshold 0.3 → 关键词兜底 |
| Top-K | 5 |
| 语料 | 129 PDF → 9432 chunks，覆盖全国 15 地区 |

---

## 基线记录

| # | 时间 | Hit Rate | MRR | Recall@10 | 平均延迟 | P50 | P95 | 策略 | 备注 |
|---|------|----------|-----|-----------|---------|-----|-----|------|------|
| 5 | 2026-06-07 09:26 | 59.0% | 0.4101 | 59.0% | 265ms | 100ms | 2839ms | 向量(embedding-3) | 紫禁城被召回但 rank 9；成都/广州部分缺失 |
| 4 | 2026-06-07 08:51 | 71.8% | 0.4868 | 71.8% | 51ms | 33ms | 206ms | **关键词** | **当前最优**，字符级 token 匹配 |
| 3 | 2026-06-07 08:50 | 0.0% | 0.0000 | 0.0% | 53ms | 34ms | 200ms | 关键词（错） | 预期标题与 PDF 实际标题不匹配 |
| 2 | 2026-06-07 08:45 | 0.0% | 0.0000 | 0.0% | 57ms | 33ms | 178ms | 关键词（错） | 分词粒度不对（整句匹配） |
| 1 | 2026-06-07 08:41 | 0.0% | 0.0000 | 0.0% | 48ms | 29ms | 190ms | 向量（全零） | embedding API 不兼容，向量全空 |

## 当前结论

**关键词 > 向量**。在当前中文旅游攻略数据上，字符级关键词匹配召回率和准确率都优于 embedding-3 语义搜索。原因是：

1. 地名/美食名等专有名词（"兵马俑"、"烤鸭"、"鼓浪屿"）关键词直接匹配即生效
2. embedding-3 的 2048 维向量对短文本相似度区分度不足
3. threshold 0.3 过滤掉部分相关结果

## 未命中分析

## 待实验方案

### 1. Chunk 参数调优

| 实验 | 参数 | 预期影响 | 优先级 |
|------|------|---------|-------|
| A | maxChars: 500→800, overlap: 80→120 | 上下文更完整 | 中 |
| B | maxChars: 500→300, overlap: 80→40 | 精确度↑，但可能丢失上下文 | 低 |
| C | overlap: 80→160 | 关键片段不丢失 | 中 |

验证：改参数 → `npx tsx src/rag/ingest.ts` 重新入库 → 跑 eval 对比。

### 2. BM25 算法替换

现状：简单词频计分 `score = 命中token数 / totalTokens`

方案：实现 BM25Okapi 算法，考虑文档长度和逆文档频率

预期 MRR +5~10%

优先级：**高**（纯算法改动，不依赖外部服务）

### 3. 修复 Embedding API

现状：`LLM_BASE_URL` 是对话 API，不支持 `/v1/embeddings`

方案 3a：加 `RAG_EMBEDDING_URL` 独立配置
- OpenAI: `text-embedding-3-small`
- 智谱: `embedding-2`

方案 3b：本地模型 `sentence-transformers/all-MiniLM-L6-v2`

优先级：中

### 4. 中文分词改进

现状：单字+双字切分，噪声大
方案：引入 jieba 分词
预期 MRR +3~5%
优先级：低

### 5. 混合搜索

方案：BM25 + 向量余弦相似度 加权融合
前置依赖：先完成实验 2 + 3
优先级：低

### 6. 语料扩充

现状：成都、杭州、重庆内容严重缺失

| 缺失城市 | 影响查询 |
|---------|---------|
| 成都 | 火锅推荐、大熊猫基地、都江堰 |
| 杭州 | 西湖、杭州攻略 |
| 重庆 | 火锅、洪崖洞 |

方案：补充 PDF 或接入百科数据
优先级：**高**

---

## 实验流程

1. 改代码/参数
2. 重新入库（如涉及 chunk/embedding 变化）
3. 运行评估：`RAG_CHROMA_URL="" node --import tsx/esm src/rag/eval.ts`
4. 记录 baseline.jsonl + 更新本日志
5. 对比指标决定是否上线
